<div class="recipes">
    {{#each this}}
    <section class="recipe">
        <div class="meta">
            {{user.username}} on {{format_date created_at}}
        </div>
        <div class="text">
            {{recipe_text}}
        </div>
    </section>
    {{/each}}
</div>