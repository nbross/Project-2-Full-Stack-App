<section class="card">
    <a href="/recipes/{{id}}">
    <header>{{recipe}}</header>

    <div class="card-body">
        <img src="{{filename}}" alt="{{description}}">
        <p>By: {{username}}</p>
    </div>
    </a>
    </section>